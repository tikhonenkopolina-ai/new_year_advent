# Telegram Advent Bot для любимого 💌

Обновлённая версия с правильной инициализацией приложения `python-telegram-bot`
для работы через вебхук и aiohttp.

Основные файлы:
- `main.py` — логика бота, вебхук-сервер и интеграция с PTB.
- `advent_content.py` — все 19 «подарков» с текстом и местами под file_id медиа.
- `requirements.txt` — зависимости.
- `render.yaml` — конфиг для деплоя на Render (free план).

Главное изменение относительно предыдущей версии:
в функции `on_startup` теперь вызывается `await bot_app.initialize()`,
что требуется в PTB v21+ для корректной работы `process_update()`.
